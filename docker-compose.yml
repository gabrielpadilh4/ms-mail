version: '3.1'

services:

  db:
    image: postgres
    volumes:
      - /private/var/lib/postgresql:/var/lib/postgresql
    ports:
      - '5432:5432'
    environment:
      POSTGRES_DB: ms_mail
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: database123